#ifndef HELP_FUNCTIONS_HPP
#define HELP_FUNCTIONS_HPP

#include "train_structs.hpp"

namespace petrov::detail
{
  int userStamina(size_t h, size_t w, size_t o);
  int exStamina(int st, size_t s, size_t r);
}

#endif
